# 贡献指南

欢迎提交问题与改进，但请保持项目的个人档案定位、0 成本原则和数据安全边界。

## 提交前

1. 不加入需要默认付费、信用卡或不可控计费的服务。
2. 不提交生产环境变量、Cloudflare 资源 ID、访客数据或数据库导出。
3. 不削弱服务端身份校验、来源检查、限流与输入边界。
4. 新增中文界面时同步补充英文界面。
5. 新增动画时支持 `prefers-reduced-motion`。
6. 变更公开行为时同步更新版本记录和测试。

## 验证

```bash
npm ci
npm test
```

提交说明应写清：

- 为什么改
- 用户会看到什么变化
- 数据库或接口是否变化
- 是否影响 0 成本边界
- 已完成哪些测试

不要在 Pull Request 中上传真实用户照片或生产数据库样本。
