# 安全政策

## 支持范围

安全修复以最新源码为准。历史构建产物和第三方分叉不在支持范围内。

## 报告漏洞

请通过正式网站公开的联系邮箱私下报告，并包含：

- 受影响页面或接口
- 可重复的最小步骤
- 预期与实际结果
- 影响范围
- 已采取的非破坏性验证方式

请不要在公开 Issue 中发布：

- 管理员密钥、Cookie、OAuth 信息或环境变量
- 访客邮箱、IP、设备标识、照片原件或数据库记录
- 可直接用于破坏生产数据的完整利用代码

## 安全边界

- 仓库不包含生产数据库与真实 secret。
- 客户端隐藏按钮不是权限控制；上传和删除必须由服务端重新核验。
- 管理接口依赖服务端 secret 或受信身份。
- 图片格式校验不等同于版权或内容合规审核。
- 任何生产数据库迁移都应先备份并在测试环境验证。
